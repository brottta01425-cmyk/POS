# Brottta POS v18.5.1 Parse Fix

Fixed the malformed JSX click handler in Billing History → REPRINT.

Incorrect:
`onClick={e=>{e.stopPropagation();printBill(g)}>`

Correct:
`onClick={e=>{e.stopPropagation();printBill(g)}}>`

No Supabase migration is required.
